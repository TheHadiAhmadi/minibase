import { defineConfig } from 'windicss/helpers';

export default defineConfig({
	darkMode: 'class',
	theme: {
		extend: {
			colors: {
				primary: '#35647a',
				secondary: '#309460',
				accent: '#b94',
				neutral: '#888'
			}
		}
	}
});
